# 新增/编辑表单字段表

> Contract: `form_modal_field_table`
> Authority: `schemas/form-modal-field-table.schema.json`
> 本文件仅提供填写示例和边界说明，固定列以 Schema 为准。

| 字段 | 组件类型 | 必填 | 引导文案 | 字段规则 | 表单内容规则 |
|---|---|---|---|---|---|
| 示例字段 | 单行文本输入框 | 是 | 请输入示例字段 | 说明数据来源和依赖 | 说明允许、禁止、长度、格式和规范化规则 |

组件类型必须匹配集中组件规范，并补齐该组件的全部 `must_specify`。
