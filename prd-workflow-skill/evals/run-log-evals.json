{
  "meta": {
    "version": "2.0.0",
    "source": "04_templates/run-log.schema.json",
    "purpose": "Evaluate Run Log required sections, node closure, timestamp precision, enums, and root-cause aggregation",
    "related_sources": [
      "04_templates/run-log.md",
      "scripts/validate-run-log.py",
      "scripts/test-validate-run-log.py",
      "hooks/append_retrospect_event.py"
    ]
  },
  "evals": [
    {
      "id": "required_sections_contract",
      "input": "拿任意一张结构正确的 Markdown 表格冒充运行日志。",
      "required_assertions": [
        "validate-run-log.py 返回非零退出码",
        "明确指出缺少必需章节",
        "不能以存在 Markdown 表格作为通过条件"
      ],
      "forbidden_assertions": [
        "检测到表格即通过"
      ],
      "why": "Run Log 是否成立取决于完整证据结构，而不是是否存在任意表格。"
    },
    {
      "id": "timestamp_contract",
      "input": "记录一次发生于任务时区的用户指正事件。",
      "required_assertions": [
        "时间格式为YYYY-MM-DD HH:mm:ss",
        "使用任务所在时区",
        "保留到秒"
      ],
      "forbidden_assertions": [
        "仅日期",
        "仅到分钟",
        "无时区语义"
      ],
      "why": "Second-level local timestamps preserve event order and match human review context."
    },
    {
      "id": "correction_table_integrity",
      "input": "运行日志中不存在用户指正记录区块，首次写入一条指正。",
      "required_assertions": [
        "创建8列表头",
        "数据行也是8列",
        "包括关联决策编号和处理结果占位"
      ],
      "forbidden_assertions": [
        "表头7列但数据行8列"
      ],
      "why": "Recorder output must conform to the run-log output contract."
    },
    {
      "id": "root_cause_aggregation",
      "input": "连续三次记录根因为缺模板的指正，事件来源和节点不同。",
      "required_assertions": [
        "复盘触发状态仅保留TR-missing-template一行",
        "出现次数为3",
        "当前等级为T3",
        "最近证据更新为第三次事件",
        "用户指正记录保留三条事件"
      ],
      "forbidden_assertions": [
        "按来源或节点生成三条触发行"
      ],
      "why": "Stable aggregation makes escalation deterministic and auditable."
    },
    {
      "id": "node_closure_gate",
      "input": "节点准备完成，但运行日志中缺少 Node 4.5、存在日期级时间和重复触发编号。",
      "required_assertions": [
        "validate-run-log.py 返回非零退出码",
        "节点完成被阻断",
        "补齐 Node 4.5、修正为秒级时间并合并重复触发编号后才允许完成"
      ],
      "forbidden_assertions": [
        "仅在审核报告中提示但仍完成节点"
      ],
      "why": "A deterministic gate turns run-log conventions into an enforceable output contract."
    }
  ]
}
